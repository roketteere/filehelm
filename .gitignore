# Node
node_modules/
dist/
.vite/

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
**/*.rs.bk
# Cargo.lock IS committed for binary crates (filehelm is a bin) so builds
# are reproducible across machines.

# Sqlx offline cache (regen with `cargo sqlx prepare`)
src-tauri/.sqlx/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
.env.*.local
.secrets/

# Local app data (shouldn't appear in repo but just in case)
.filehelm/
